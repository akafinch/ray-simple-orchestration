# REFERENCE ONLY — this file is not applied directly.
# The deploy script generates the ConfigMap dynamically from Terraform outputs:
#
#   kubectl create configmap inference-config \
#     --from-literal=ray-serve-url="http://clip-clap-serve-svc.ray-system:8000" \
#     --from-literal=obj-endpoint="<from terraform output obj_endpoint>" \
#     --from-literal=model-bucket="<from terraform output model_bucket>"
#
# This ensures the OBJ endpoint always matches the actual storage region
# without hardcoding. See scripts/deploy.sh Phase 4.
